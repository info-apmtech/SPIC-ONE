Put eng.traineddata here.

Download it once:
  curl -L -o eng.traineddata https://github.com/tesseract-ocr/tessdata_fast/raw/main/eng.traineddata

Without it the OCR solver logs an error and every CAPTCHA falls through to the
Android app prompt.
