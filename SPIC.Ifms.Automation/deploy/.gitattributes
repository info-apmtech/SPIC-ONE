# Shell scripts and unit files must keep LF endings or Linux refuses to run
# them: a CRLF shebang produces "bad interpreter: /bin/bash^M", which is a
# genuinely baffling error the first time you meet it.
*.sh text eol=lf
*.service text eol=lf
*.env text eol=lf
