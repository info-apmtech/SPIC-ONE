<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:supportsRtl="true" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<!--
		IFMS OTP relay. These let the phone holding the IFMS SIM forward the
		one-time password to SpicAPI so the 04:05 login needs nobody awake.

		Note for distribution: Google Play restricts RECEIVE_SMS/READ_SMS to apps
		whose core function is messaging, so a build carrying these is for internal
		sideloading or a managed enterprise channel, not the public Play Store. The
		relay also stays switched off until the phone is paired on the IFMS Relay
		screen, so installing the app does not by itself read anyone's messages.
	-->
	<uses-permission android:name="android.permission.RECEIVE_SMS" />
	<uses-permission android:name="android.permission.READ_SMS" />

	<!-- Restart the CAPTCHA watcher after a reboot. -->
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

	<!-- The watcher runs as a foreground service so Android does not stop it. -->
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

	<!-- Android 13+ needs this before any notification is shown. -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>