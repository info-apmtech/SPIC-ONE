@* <div class="mb-4">

	<!-- TOP ROW -->
	<div class="d-flex justify-content-between align-items-center">
		<div class="step-text">Step 1 of 12</div>
		<span class="status-badge">Registration In Progress</span>
	</div>

	<!-- SUB TEXT -->
	<div class="step-subtext">Market Details & Territory Coverage</div>

	<!-- PROGRESS BAR -->
	<div class="progress-bar-custom">
		<div class="progress-segment active"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
		<div class="progress-segment"></div>
	</div>
</div> *@

@* <div class="mb-4">

	<div class="d-flex justify-content-between align-items-center">
		<div class="step-text">Step @CurrentStep of @TotalSteps</div>
		<span class="status-badge">@StatusText</span>
	</div>

	<div class="step-subtext">@StepTitle</div>

	<div class="progress-bar-custom">
		@for (int i = 1; i <= TotalSteps; i++)
		{
			<div class="progress-segment @(i <= CurrentStep ? "active" : "")"></div>
		}
	</div>
</div>

@code {
	[Parameter] public int CurrentStep { get; set; }
	[Parameter] public int TotalSteps { get; set; } = 12;
	[Parameter] public string StepTitle { get; set; } = "";
	[Parameter] public string StatusText { get; set; } = "Registration In Progress";
} *@

@using SPIC.MauiBlazorApp.Shared.FormFlow
@inject NavigationManager Nav

@if (CurrentStep is not null)
{
	<div class="mb-4">
		<div class="d-flex justify-content-between align-items-center">
			<div class="step-text">Step @CurrentStep.StepNo of @TotalSteps</div>
			<span class="status-badge">Registration In Progress</span>
		</div>

		<div class="step-subtext">@CurrentStep.Title</div>

		<div class="progress-bar-custom">
			@for (int i = 1; i <= TotalSteps; i++)
			{
				<div class="progress-segment @(i <= CurrentStep.StepNo ? "active" : "")"></div>
			}
		</div>
	</div>
}

@code {
	private FormStepModel? CurrentStep;
	private int TotalSteps => FormStepData.Steps.Count;

	protected override void OnInitialized()
	{
		var relativePath = "/" + Nav.ToBaseRelativePath(Nav.Uri).Split('?')[0];
		CurrentStep = FormStepData.GetStepByRoute(relativePath);
	}
}