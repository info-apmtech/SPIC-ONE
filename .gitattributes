# Files copied into Linux container images must keep LF endings on every checkout,
# whatever core.autocrlf says; a CRLF shebang breaks the backup job at runtime.
deploy/azure/backup/backup.sh text eol=lf
deploy/azure/backup/Dockerfile text eol=lf
